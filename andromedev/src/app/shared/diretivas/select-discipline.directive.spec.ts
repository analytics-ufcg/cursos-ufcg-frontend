import { SelectDisciplineDirective } from './select-discipline.directive';

describe('SelectDisciplineDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectDisciplineDirective();
    expect(directive).toBeTruthy();
  });
});
